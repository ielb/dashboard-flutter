import 'package:dashboard/src/core/extensions/extensions.dart';

class Analytic {
  String title;
  String value;
  IconData icon;
  Color color;

  Analytic(
      {required this.title,
      required this.value,
      required this.icon,
      required this.color});
}
